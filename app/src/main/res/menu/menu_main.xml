<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/menu_average"
        android:title="Get The average"
        app:showAsAction="never" />
    <item
        android:id="@+id/menu_select_questions"
        android:title="Select the number of questions"
        app:showAsAction="never" />
    <item
        android:id="@+id/menu_reset_results"
        android:title="Reset the saved results"
        app:showAsAction="never" />
</menu>
